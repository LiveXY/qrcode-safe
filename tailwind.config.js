/** @type {import('tailwindcss').Config} */
export default {
  // Config disabled to fix build errors. 
  // Configuration is now handled in index.html via CDN script.
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
}